if (SLOG_JOURNALD)
    file(GLOB source CONFIGURE_DEPENDS *.cpp)
    add_compile_options(-pg)
    add_executable(slogBenchmark bench.cpp)
    target_link_libraries(slogBenchmark PUBLIC slog)
else()
    message(STATUS "Benchmark requires journald")
endif()